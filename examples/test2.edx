SEND 'Enter an integer: ' TO DISPLAY
RECEIVE theNumber FROM (INTEGER) KEYBOARD

IF (theNumber < 40) THEN
  SEND 'Low range' TO DISPLAY
END IF

IF (theNumber > 39) AND (theNumber < 70) THEN
  SEND 'Middle range' TO DISPLAY
END IF

IF (theNumber > 69) THEN
  SEND 'High range' TO DISPLAY
END IF